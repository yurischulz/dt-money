import styled from 'styled-components';

export const MoneyContainer = styled.div`
  font-size: 1rem;
  font-weight: 500;
  line-height: 1rem;

  > span {
    vertical-align: text-top;
    font-size: 0.6rem;
  }
`;
